// <auto-generated/>
// DO NOT EDIT — generated by Rerun.Net.CodeGen
#pragma warning disable CS0542 // member names shall not match enclosing type

using Rerun.Net.Components;

namespace Rerun.Net.Archetypes;

/// <summary>
/// 3D ellipsoids or spheres.
/// 
/// This archetype is for ellipsoids or spheres whose size is a key part of the data
/// (e.g. a bounding sphere).
/// For points whose radii are for the sake of visualization, use [archetypes.Points3D] instead.
/// 
/// If there's more instance poses than half sizes, the last ellipsoid/sphere's orientation will be repeated for the remaining poses.
/// Orienting and placing ellipsoids/spheres forms a separate transform that is applied prior to [archetypes.InstancePoses3D] and [archetypes.Transform3D].
/// 
/// </summary>
public sealed partial class Ellipsoids3D : IAsComponents
{
    private const string ArchetypeName = "rerun.archetypes.Ellipsoids3D";

    public ComponentBatch HalfSizes { get; private set; }
    public ComponentBatch? Centers { get; private set; }
    public ComponentBatch? Colors { get; private set; }
    public ComponentBatch? RotationAxisAngles { get; private set; }
    public ComponentBatch? Quaternions { get; private set; }
    public ComponentBatch? LineRadii { get; private set; }
    public ComponentBatch? FillMode { get; private set; }
    public ComponentBatch? Labels { get; private set; }
    public ComponentBatch? ShowLabels { get; private set; }
    public ComponentBatch? ClassIds { get; private set; }

    public Ellipsoids3D(ReadOnlySpan<Components.HalfSize3D> half_sizes)
    {
        HalfSizes = ComponentBatch.FromLoggable(half_sizes,
            new ComponentDescriptor(ArchetypeName, "Ellipsoids3D:half_sizes", "rerun.components.HalfSize3D"));
    }

    public Ellipsoids3D(params Components.HalfSize3D[] half_sizes) : this(half_sizes.AsSpan()) { }

    public Ellipsoids3D WithCenters(ReadOnlySpan<Components.Translation3D> centers)
    {
        Centers = ComponentBatch.FromLoggable(centers,
            new ComponentDescriptor(ArchetypeName, "Ellipsoids3D:centers", "rerun.components.Translation3D"));
        return this;
    }

    public Ellipsoids3D WithCenters(params Components.Translation3D[] centers) => WithCenters(centers.AsSpan());

    public Ellipsoids3D WithColors(ReadOnlySpan<Components.Color> colors)
    {
        Colors = ComponentBatch.FromLoggable(colors,
            new ComponentDescriptor(ArchetypeName, "Ellipsoids3D:colors", "rerun.components.Color"));
        return this;
    }

    public Ellipsoids3D WithColors(params Components.Color[] colors) => WithColors(colors.AsSpan());

    public Ellipsoids3D WithRotationAxisAngles(ReadOnlySpan<Components.RotationAxisAngle> rotation_axis_angles)
    {
        RotationAxisAngles = ComponentBatch.FromLoggable(rotation_axis_angles,
            new ComponentDescriptor(ArchetypeName, "Ellipsoids3D:rotation_axis_angles", "rerun.components.RotationAxisAngle"));
        return this;
    }

    public Ellipsoids3D WithRotationAxisAngles(params Components.RotationAxisAngle[] rotation_axis_angles) => WithRotationAxisAngles(rotation_axis_angles.AsSpan());

    public Ellipsoids3D WithQuaternions(ReadOnlySpan<Components.RotationQuat> quaternions)
    {
        Quaternions = ComponentBatch.FromLoggable(quaternions,
            new ComponentDescriptor(ArchetypeName, "Ellipsoids3D:quaternions", "rerun.components.RotationQuat"));
        return this;
    }

    public Ellipsoids3D WithQuaternions(params Components.RotationQuat[] quaternions) => WithQuaternions(quaternions.AsSpan());

    public Ellipsoids3D WithLineRadii(ReadOnlySpan<Components.Radius> line_radii)
    {
        LineRadii = ComponentBatch.FromLoggable(line_radii,
            new ComponentDescriptor(ArchetypeName, "Ellipsoids3D:line_radii", "rerun.components.Radius"));
        return this;
    }

    public Ellipsoids3D WithLineRadii(params Components.Radius[] line_radii) => WithLineRadii(line_radii.AsSpan());

    public Ellipsoids3D WithFillMode(ReadOnlySpan<Components.FillMode> fill_mode)
    {
        FillMode = ComponentBatch.FromLoggable(fill_mode,
            new ComponentDescriptor(ArchetypeName, "Ellipsoids3D:fill_mode", "rerun.components.FillMode"));
        return this;
    }

    public Ellipsoids3D WithFillMode(params Components.FillMode[] fill_mode) => WithFillMode(fill_mode.AsSpan());

    public Ellipsoids3D WithLabels(ReadOnlySpan<Components.Text> labels)
    {
        Labels = ComponentBatch.FromLoggable(labels,
            new ComponentDescriptor(ArchetypeName, "Ellipsoids3D:labels", "rerun.components.Text"));
        return this;
    }

    public Ellipsoids3D WithLabels(params Components.Text[] labels) => WithLabels(labels.AsSpan());

    public Ellipsoids3D WithShowLabels(ReadOnlySpan<Components.ShowLabels> show_labels)
    {
        ShowLabels = ComponentBatch.FromLoggable(show_labels,
            new ComponentDescriptor(ArchetypeName, "Ellipsoids3D:show_labels", "rerun.components.ShowLabels"));
        return this;
    }

    public Ellipsoids3D WithShowLabels(params Components.ShowLabels[] show_labels) => WithShowLabels(show_labels.AsSpan());

    public Ellipsoids3D WithClassIds(ReadOnlySpan<Components.ClassId> class_ids)
    {
        ClassIds = ComponentBatch.FromLoggable(class_ids,
            new ComponentDescriptor(ArchetypeName, "Ellipsoids3D:class_ids", "rerun.components.ClassId"));
        return this;
    }

    public Ellipsoids3D WithClassIds(params Components.ClassId[] class_ids) => WithClassIds(class_ids.AsSpan());

    /// <summary>Create an empty Ellipsoids3D for partial updates. Use With*() to set fields.</summary>
    public static Ellipsoids3D UpdateFields() => new();

    /// <summary>Clear all fields, then use With*() to set specific ones.</summary>
    public static Ellipsoids3D ClearFields() => new();

    private Ellipsoids3D() { }

    public IReadOnlyList<ComponentBatch> AsBatches()
    {
        var batches = new List<ComponentBatch>(10);
        batches.Add(HalfSizes);
        if (Centers != null) batches.Add(Centers);
        if (Colors != null) batches.Add(Colors);
        if (RotationAxisAngles != null) batches.Add(RotationAxisAngles);
        if (Quaternions != null) batches.Add(Quaternions);
        if (LineRadii != null) batches.Add(LineRadii);
        if (FillMode != null) batches.Add(FillMode);
        if (Labels != null) batches.Add(Labels);
        if (ShowLabels != null) batches.Add(ShowLabels);
        if (ClassIds != null) batches.Add(ClassIds);
        return batches;
    }
}
