# MinimalExample

Log Points3D with colors and radii over 2 animated frames.

## Archetypes

- `Points3D` with `Position3D`, `Color`, `Radius`

## Run

```bash
dotnet run --project examples/MinimalExample
dotnet run --project examples/MinimalExample -- --spawn   # opens Rerun viewer
```
