# ScalarPlot

Log sin, cos, and sin*cos scalar curves over 200 time steps.

## Archetypes

- `Scalars`

## Run

```bash
dotnet run --project examples/ScalarPlot
dotnet run --project examples/ScalarPlot -- --spawn   # opens Rerun viewer
```
