# Minimal

Log a 10x10x10 grid of 1000 colored 3D points.

## Archetypes

- `Points3D` with `Position3D`, `Color`, `Radius`

## Run

```bash
dotnet run --project examples/Minimal
dotnet run --project examples/Minimal -- --spawn   # opens Rerun viewer
```
