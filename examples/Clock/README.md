# Clock

Animated analog clock with second, minute, and hour hands using 3D arrows.

## Archetypes

- `Points3D` (static hour markers on the clock face)
- `Arrows3D` (animated second, minute, and hour hands)

## Run

```bash
dotnet run --project examples/Clock
dotnet run --project examples/Clock -- --spawn   # opens Rerun viewer
```
