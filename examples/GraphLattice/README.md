# GraphLattice

A 10x10 lattice graph with gradient-colored nodes and directed edges.

## Archetypes

- `GraphNodes` with labels and colors
- `GraphEdges` with directed graph type

## Run

```bash
dotnet run --project examples/GraphLattice
dotnet run --project examples/GraphLattice -- --spawn   # opens Rerun viewer
```
