# LiveScrollingPlot

6 plots with 5 random-walk series each, logged over 500 time steps (15,000 total scalars).

## Archetypes

- `Scalars`

## Run

```bash
dotnet run --project examples/LiveScrollingPlot
dotnet run --project examples/LiveScrollingPlot -- --spawn   # opens Rerun viewer
```
