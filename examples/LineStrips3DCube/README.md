# LineStrips3DCube

A wireframe cube drawn with colored 3D line strips (red bottom, green top, blue verticals).

## Archetypes

- `LineStrips3D` with `Color`

## Run

```bash
dotnet run --project examples/LineStrips3DCube
dotnet run --project examples/LineStrips3DCube -- --spawn   # opens Rerun viewer
```
