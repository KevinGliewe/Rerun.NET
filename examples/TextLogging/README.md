# TextLogging

Log text entries at different severity levels (INFO, DEBUG, WARN, ERROR).

## Archetypes

- `TextLog` with `Text` and `TextLogLevel`

## Run

```bash
dotnet run --project examples/TextLogging
dotnet run --project examples/TextLogging -- --spawn   # opens Rerun viewer
```
